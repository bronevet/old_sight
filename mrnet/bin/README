==========================================================================================================
Starting the Front End MRNet
==========================================================================================================
a.) first export LD_LIBRARY_PATH to point to the MRNet libraries. You can easily do this with following
    command

    source ../lib/sourced.file

b.) initiate the MRnet Front-End

    ./smrnet_fe top_file_s2 libsmrnet_filter.so 4



==========================================================================================================
Starting the Emitter for MRNet Merge
==========================================================================================================
a.) first export LD_LIBRARY_PATH to point to the MRNet libraries. You can easily do this with following
    command

    source ../lib/sourced.file

b.) modify "app.params" file appropriately. Each line represent the parameters for sight application
      
          #executable/binary    #host      #app parameters  
   ie:- ./12.SampleMRnetEmitter 127.0.0.1  p1 p2   
 
   for example if 4 application instances are connecting to MRNet tree then 4 lines should be present.

c.) users should connet MRnet Backend emiitters to the already started MRnet Front-End. The file app.params
should be modified correctly before connecting.

   ./connect.pl

optionally users can give connection file and a app.params file to the program.

   ./connect.pl [connections.file] [app.parameters file]

